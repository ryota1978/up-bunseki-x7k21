# セットアップ手順（依頼書 第7章「進め方」の第2段階に対応）

ここから先は、Claude Codeと一緒に1つずつ進めてください。慣れない言葉には説明を添えています。

## 0. 今の状態

コードはもう書き上がっています（`npm run build` が通ることを確認済みです）。
ここからは、**あなたの「データベース」と「公開先」のアカウントを用意して、コードとつなぐ作業**です。
一度つなげば、以後は同じURLのまま更新され続けます（依頼書 第1章の教訓）。

必要なアカウントは3つです。

| アカウント | 役割 | 費用 |
|---|---|---|
| GitHub | コードの保管場所（すでにお持ちです） | 無料 |
| Supabase | データベース（案件・メンバーの保存先） | 無料枠 |
| Vercel | 公開先（アプリのURLを発行する場所） | 無料枠 |

---

## 1. Supabaseでプロジェクトを作る

1. https://supabase.com を開き、「Start your project」からGitHubアカウントでサインアップ
2. 「New project」を押す
3. 名前は自由（例：up-bunseki）、パスワードは自動生成でOK（控えておいてください）
4. リージョン（データを置く地域）は「Northeast Asia (Tokyo)」を選ぶ
5. 「Create new project」を押す（1〜2分待ちます）

### データベースの表（テーブル）を作る

1. 左メニューの「SQL Editor」を開く
2. このリポジトリの `supabase/schema.sql` の中身を全部コピーして貼り付ける
3. 右下の「Run」を押す
4. エラーが出ずに終われば完了です（メンバー29組も自動で登録されます）

### 接続情報を控える

1. 左メニューの歯車アイコン「Project Settings」→「Data API」を開く
   - 「Project URL」をコピー →これが `NEXT_PUBLIC_SUPABASE_URL`
2. 同じ画面の「API Keys」タブで「anon public」の値をコピー →これが `NEXT_PUBLIC_SUPABASE_ANON_KEY`

---

## 2. Vercelでプロジェクトを作る

1. https://vercel.com を開き、GitHubアカウントでサインアップ
2. 「Add New...」→「Project」
3. このGitHubリポジトリ（`up-bunseki-x7k21`）を選んで「Import」
4. まだ「Deploy」は押さず、先に環境変数を入力します（次の章）

---

## 3. 環境変数を入力する

Vercelの「Environment Variables」の画面で、以下を1つずつ入力します
（`.env.example` にも同じ説明があります）。

| 名前 | 値 |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | 手順1で控えたProject URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 手順1で控えたanon public キー |
| `APP_PASSPHRASE` | ログイン画面で入力する合言葉。好きな文字列を決めてください |
| `ANTHROPIC_API_KEY` | （任意）音声入力の自動読み取りを使う場合のみ。https://console.anthropic.com/ で発行 |

入力できたら「Deploy」を押します。1〜2分でURLが発行されます。

**このURLが、加藤さん・助田さんに渡す「最初から最後まで1つだけ」のリンクです。**
以後コードを直しても、このURLは変わりません。

---

## 4. 動作確認（依頼書 第7章の各段階に対応）

- [ ] 発行されたURLを開き、合言葉→名前選択でアプリに入れる
- [ ] 案件の追加・済チェックができる
- [ ] 別の端末（スマホとPC）で同時に開き、片方で操作するともう片方にも反映される
- [ ] iPhoneのSafariで開き、共有ボタン→「ホーム画面に追加」→アイコンから正しくアプリが開く
- [ ] スマホで「音声で追加」を押し、実際にマイクが使えるか確認する
- [ ] LINE WORKSでこのURLを送り、内蔵ブラウザで開いて動作するか確認する
  - もし内蔵ブラウザで崩れる場合は、画面右上のメニューなどから「外部ブラウザで開く」を選ぶよう、
    加藤さん・助田さんに案内してください
- [ ] 印刷ボタンから実際に紙に印刷してみる

---

## 5. 以後の更新について

コードを直した後は、GitHubにプッシュするだけで、Vercelが自動で同じURLに反映します。
URLを毎回発行し直す必要はありません（これが今回の一番の目的です）。
